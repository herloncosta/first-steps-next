import styled from "../styles/styled.module.css";

export default function Styled() {
	return (
		<div className={styled.box}>
			<h1 className={styled.title}>Herlon Costa Dev</h1>
			<h2 className={styled.subtitle}>
				O melhor canal de tecnologia pra você que está começando!
			</h2>
			<p>Seja muito bem vindo, pegue o seu café e vamos codar!</p>
		</div>
	);
}
